// Redirects for old URLs. Empty for now; add { source, destination, permanent }
// entries here if any external links to retired pages need to be preserved.
export function getLegacyRedirects(): { source: string; destination: string; permanent: boolean }[] {
  return [];
}
